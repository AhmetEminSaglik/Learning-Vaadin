/*
package org.vaadin.aes.util;

import com.vaadin.flow.component.UI;
import com.vaadin.flow.component.textfield.TextField;
import org.vaadin.aes.model.EnumI18NLanguageSupport;

public class CurrentLanguage {
    public static String getCurrentTranslateText(EnumI18NLanguageSupport enumData) {
        return UI.getCurrent().getTranslation(enumData.getName());
    }

    public static TextField setCurrentLanguageToTxtField(TextField textField, EnumI18NLanguageSupport enumData) {
        String text = getCurrentTranslateText(enumData);
        textField.setLabel(text);
        return textField;
    }
}
*/
